server.port=8080
spring.application.name=producttracker

# Supabase PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://aws-1-eu-north-1.pooler.supabase.com:5432/postgres?sslmode=require
spring.datasource.username=postgres.fcsnlthcawwpeuvpbwic
spring.datasource.password=ProductTracer2025
spring.datasource.driver-class-name=org.postgresql.Driver

# Hikari Connection Pool Configuration for resilience
spring.datasource.hikari.connection-test-query=SELECT 1
spring.datasource.hikari.keepalive-time=60000
spring.datasource.hikari.max-lifetime=290000

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.sql.init.mode=always
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Kafka Configuration
spring.kafka.bootstrap-servers=${SPRING_KAFKA_BOOTSTRAP_SERVERS:localhost:9092}

# Notification Service Configuration
low.stock.threshold=10

# Debug Logging
logging.level.org.springframework=DEBUG
