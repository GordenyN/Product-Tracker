# Система отслеживания товаров

Это многомодульное приложение на Java (Spring Boot) для отслеживания товаров на складе. Система позволяет управлять каталогом товаров и категорий, а также отправляет уведомления в Telegram при низком остатке товара на складе.

## Архитектура

Проект состоит из двух основных сервисов:

*   `product-service`: Основной сервис, предоставляющий REST API для управления товарами и категориями. Он взаимодействует с базой данных PostgreSQL.
*   `notification-service`: Сервис, отвечающий за отправку уведомлений. Он получает сообщения от `product-service` через Apache Kafka и отправляет их в Telegram.

Взаимодействие между сервисами осуществляется с помощью Apache Kafka, что обеспечивает асинхронность и отказоустойчивость.

## Технологии

*   **Бэкенд:** Java 17, Spring Boot
*   **База данных:** PostgreSQL
*   **Обмен сообщениями:** Apache Kafka
*   **Сборка:** Maven
*   **Контейнеризация:** Docker, Docker Compose
*   **Уведомления:** Telegram Bot API

## Функционал

*   **CRUD-операции для товаров:** Создание, чтение, обновление и удаление товаров.
*   **CRUD-операции для категорий:** Создание, чтение, обновление и удаление категорий.
*   **Получение товаров по категории:** Возможность фильтрации товаров по их категории.
*   **Асинхронные уведомления:** Автоматическая отправка уведомлений в Telegram, когда количество товара на складе становится ниже определенного порога.
*   **Синхронный и асинхронный режимы:** API для обновления товаров поддерживает как синхронный, так и асинхронный (через Kafka) режимы.

## API Эндпоинты

### Категории (`/api/v1/categories`)

*   `GET /`: Получить все категории.
*   `GET /{id}`: Получить категорию по ID.
*   `POST /`: Создать новую категорию.
*   `PUT /{id}`: Обновить категорию.
*   `DELETE /{id}`: Удалить категорию.

### Товары (`/api/v1/products`)

*   `GET /`: Получить все товары.
*   `GET /{id}`: Получить товар по ID.
*   `GET /category/{categoryId}`: Получить все товары в указанной категории.
*   `POST /`: Создать новый товар.
*   `PUT /sync/{id}`: Обновить товар (синхронный режим).
*   `POST /async/update`: Обновить товар (асинхронный режим, через Kafka).
*   `DELETE /{id}`: Удалить товар.

## Как запустить проект

1.  **Склонируйте репозиторий:**
    ```bash
    git clone <URL вашего репозитория>
    cd product-tracker-parent
    ```

2.  **Настройте переменные окружения:**
    Создайте файл `.env` в корневой директории проекта и укажите в нем следующие переменные:
    ```
    TELEGRAM_BOT_TOKEN=ВАШ_ТЕЛЕГРАМ_БОТ_ТОКЕН
    TELEGRAM_CHAT_ID=ВАШ_ТЕЛЕГРАМ_ЧАТ_ID
    ```

3.  **Запустите проект с помощью Docker Compose:**
    ```bash
    docker-compose up --build
    ```
    Эта команда соберет образы для всех сервисов, создаст необходимые контейнеры (включая PostgreSQL и Kafka) и запустит их.

## Примеры использования API

Для демонстрации работы API вы можете использовать следующие команды `curl`.
Убедитесь, что ваш проект запущен через `docker-compose up`.

### Подготовка JSON-файлов

Для некоторых команд `curl` требуются JSON-файлы с данными. Создайте в корне проекта следующие файлы:

**`product.json` (для создания продукта):**
```json
{
  "name": "Новый Продукт",
  "description": "Описание нового продукта",
  "price": 99.99,
  "quantity": 100,
  "categoryId": null
}
```

**`update.json` (для обновления продукта):**
```json
{
  "name": "Обновленный Продукт",
  "description": "Обновленное описание продукта",
  "price": 120.50,
  "quantity": 50,
  "categoryId": null
}
```

**`category.json` (для создания категории):**
```json
{
  "name": "Электроника",
  "description": "Категория для электронных устройств"
}
```

**`category-update.json` (для обновления категории):**
```json
{
  "name": "Бытовая Электроника",
  "description": "Обновленная категория для бытовой электроники"
}
```

### 1. Создание продукта (Create)

```bash
curl -X POST -H "Content-Type: application/json" -d @product.json http://localhost:8080/api/v1/products
```
🟢 Ожидаемый результат:

<img width="2494" height="155" alt="1" src="https://github.com/user-attachments/assets/d2a5edbe-f9c4-44de-ae8b-1e491b268dd4" />


---

### 2. Получение всех продуктов (Read All)

Проверьте, что ваш новый продукт появился в списке:

```bash
curl http://localhost:8080/api/v1/products
```
🟢 Ожидаемый результат:
Список всех продуктов в формате JSON, включая тот, который вы только что создали.

---

### 3. Получение продукта по ID (Read One)

Получите конкретный продукт по его id (используйте id со шага 1):

```bash
curl http://localhost:8080/api/v1/products/1
```
🟢 Ожидаемый результат:
JSON-объект только что созданного продукта.

---

### 4. Обновление продукта (Update)

```bash
curl -X PUT -H "Content-Type: application/json" -d @update.json http://localhost:8080/api/v1/products/1
```
🟢 Ожидаемый результат:
JSON-объект обновлённого продукта.

---

### 5. Проверка обновлённого продукта (Read Updated)

Убедитесь, что изменения применились:

```bash
curl http://localhost:8080/api/v1/products/1
```
🟢 Ожидаемый результат:
JSON-объект продукта с новыми данными.

---

### 6. Удаление продукта (Delete)

Удалите продукт по его id:

```bash
curl -X DELETE http://localhost:8080/api/v1/products/1
```
🟢 Ожидаемый результат:
Успешное удаление обычно возвращает пустой ответ или статус 204 No Content.

---

### 7. Проверка удаления (Read All Again)

Убедитесь, что продукт был удалён и больше не отображается в списке:

```bash
curl http://localhost:8080/api/v1/products
```
🟢 Ожидаемый результат:
Список всех продуктов без удалённого элемента.

---

### 💡 Дополнительно

Вы также можете протестировать CRUD-операции для категорий по адресу: `http://localhost:8080/api/v1/categories`
Используйте аналогичные команды `curl` для проверки создания, чтения, обновления и удаления категорий.

---
## 📂 Демонстрация CRUD-операций с категориями

Вы можете протестировать работу API для категорий по адресу:
`http://localhost:8080/api/v1/categories`

Перед началом убедитесь, что сервер запущен.

---

### 1. Создание категории (Create)

```bash
curl -X POST -H "Content-Type: application/json" -d @category.json http://localhost:8080/api/v1/categories
```
🟢 Ожидаемый результат:
В ответ вы получите JSON-объект созданной категории, включая её id.


---

### 2. Получение всех категорий (Read All)

Проверьте, что новая категория появилась в списке:

```bash
curl http://localhost:8080/api/v1/categories
```
🟢 Ожидаемый результат:
Список всех категорий в формате JSON, включая только что созданную.

---

### 3. Получение категории по ID (Read One)

Получите конкретную категорию по её id:

```bash
curl http://localhost:8080/api/v1/categories/1
```
🟢 Ожидаемый результат:
JSON-объект категории с указанным id.

---

### 4. Обновление категории (Update)

```bash
curl -X PUT -H "Content-Type: application/json" -d @category-update.json http://localhost:8080/api/v1/categories/1
```
🟢 Ожидаемый результат:
JSON-объект обновлённой категории.

---

### 5. Проверка обновлённой категории (Read Updated)

Убедитесь, что изменения применились:

```bash
curl http://localhost:8080/api/v1/categories/1
```
🟢 Ожидаемый результат:
JSON-объект категории с обновлёнными данными.

---

### 6. Удаление категории (Delete)

Удалите категорию по её id:

```bash
curl -X DELETE http://localhost:8080/api/v1/categories/1
```
🟢 Ожидаемый результат:
Успешное удаление обычно возвращает статус 204 No Content.

---

### 7. Проверка удаления (Read All Again)

Проверьте, что категория больше не отображается в списке:

```bash
curl http://localhost:8080/api/v1/categories
```
🟢 Ожидаемый результат:
Список всех категорий без удалённой записи.





**Пример сообщения в Telegram:**
<img width="754" height="953" alt="12" src="https://github.com/user-attachments/assets/5e581510-030b-46d0-9123-edef0ee9b611" />

<img width="739" height="408" alt="13" src="https://github.com/user-attachments/assets/fbc2e739-9c02-4201-9ede-6ca73cccede2" />

<img width="696" height="479" alt="14" src="https://github.com/user-attachments/assets/5a7b60ab-1e60-49c4-8757-6da9b3beee22" />

